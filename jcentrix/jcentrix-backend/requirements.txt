# ─────────────────────────────────────────────────────────
# JCENTRIX BACKEND — Python Dependencies
# ─────────────────────────────────────────────────────────

# FastAPI: Our web framework. Creates routes, handles requests/responses.
fastapi==0.111.0

# Uvicorn: The ASGI server that actually RUNS FastAPI (like Apache for Flask).
uvicorn[standard]==0.29.0

# SQLAlchemy: ORM — lets us write Python classes instead of raw SQL.
# "async" version is included via asyncpg driver below.
sqlalchemy==2.0.30

# Alembic: Database migration tool — tracks schema changes over time.
# Think of it as "Git for your database tables".
alembic==1.13.1

# aiomysql: High-performance async MySQL driver for SQLAlchemy async sessions.
aiomysql==0.2.0

# PyMySQL: Sync MySQL driver (used by Alembic for migrations).
PyMySQL==1.1.0

# Pydantic: Data validation library. FastAPI uses it for request/response schemas.
pydantic==2.7.1

# pydantic-settings: Loads .env files into Pydantic models (for config).
pydantic-settings==2.2.1

# python-jose: Creates and verifies JWT tokens (our authentication tokens).
python-jose[cryptography]==3.3.0

# passlib: Password hashing library (we use bcrypt algorithm for security).
passlib==1.7.4

bcrypt==3.2.2

# python-multipart: Required for FastAPI to handle form data and file uploads.
python-multipart==0.0.9

# python-dotenv: Loads .env file into environment variables.
python-dotenv==1.0.1

# httpx: Async HTTP client (used internally by some FastAPI test utilities).
httpx==0.27.0

# groq: Official Groq Python SDK — used in interview_service.py for AI interview
# questions, evaluation, and feedback generation (Llama 3.3-70b model).
groq==0.8.0

